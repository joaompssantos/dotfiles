##### Aliases file #####

### portage ###
alias world-update='smart-live-rebuild ; emerge --update --newuse --changed-use --deep @world'

### ls ###
alias ls='ls --color=auto'
alias ll='ls -lFh'           # size,show type,human readable
alias la='ls -lAFh'          # long list,show almost all,show type,human readable
alias lr='ls -tRFh'          # sorted by date,recursive,show type,human readable
alias lt='ls -ltFh'          # long list,sorted by date,show type,human readable
alias l='ls -l'              # long list
alias ldot='ls -ld .*'
alias lS='ls -1FSsh'
alias lart='ls -1Fcart'
alias lrt='ls -1Fcrt'

### htop ###
alias utop='htop -u $USER'

### sudoedit ###
alias sudoedit='SUDO_EDITOR=kate sudoedit'

### Inspiration: https://github.com/Phantas0s/.dotfiles/blob/master/aliases/aliases
